#include <iostream>

using namespace std;

int main() {
  ios_base::sync_with_stdio(false);
  int n, m, p, k;
  // liczbę miejscowości, liczbę łączących je dróg, liczbę gatunków potworów i
  // liczbę kowali
  cin >> n >> m >> p >> k;

  return 0;
}